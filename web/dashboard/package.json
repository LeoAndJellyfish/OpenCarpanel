{
  "name": "@opencarpanel/dashboard",
  "private": true,
  "version": "0.2.1",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "vite --host",
    "test": "vitest run"
  },
  "dependencies": {
    "@opencarpanel/widget-sdk": "0.2.1",
    "preact": "10.29.8"
  }
}
