{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capabilities for bundled OpenCarpanel control-center code.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
